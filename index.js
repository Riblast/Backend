const Contenedor = require('./Clase')
const productos = new Contenedor('./productos.json')

const ejecutar = async () =>{

    const item1 = {
        itemName: "Pizzeta integral",
        itemPrice: 200,
        url: "url"
    }

}
ejecutar()